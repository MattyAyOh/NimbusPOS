import React from "react"
import styled from "styled-components"

const SingleColumnLayout = styled.div`
  display: grid;
  grid-row-gap: 2rem;
  margin: 0px 20rem;
`

SingleColumnLayout.area = (row, column) => (
  `grid-area: ${row} / ${column}`
)

export default SingleColumnLayout
