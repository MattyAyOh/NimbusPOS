import React from "react"
import styled from "styled-components"

class Lobby extends React.Component {
  render () {
    return (
      <div>
      This is where the available & taken rooms are displayed.
      </div>
    );
  }
}

export default Lobby
